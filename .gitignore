# MCP config (contains API keys)
.mcp.json

# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
test-results/
playwright-report/

# Next.js
.next/
out/

# Production
build/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env
.env.prod
.env.vercel
.env.check
.env.vercel-check

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Claude (root and any nested)
.claude/
**/.claude/

# One-off scripts and temp files
scripts/
tmp_*.json
hl_lb.json
cookies.txt
nul
*.tmp.check
C:*

.vercel

src/remotion/

# Claude/Codex plugin installs — sometimes drop their skill bundles
# into .agents/skills/* in this repo root. Not part of the app.
.agents/

# Local design-handoff scratch (PNG/SVG drops, screenshots, prototypes)
.design-handoff/

# Throwaway QA screenshots from agent runs — .tmp-* / .verify-* /
# .codex-* / .deslop-* / .improve-* / .ui-* — the actual filename
# prefix changes per agent but they're all dropped at repo root and
# never want to be committed.
.tmp-*.png
.verify-*.png
.codex-*.png
.deslop-*.png
.improve-*.png
.ui-*.png
.codex-next-dev.*
.next-dev.*

# Other dotfile scratch
.hl-whales.json
.env.production-pull
