<svg width="512" height="512" viewBox="0 0 512 512" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="brandGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#FFD700"/>
      <stop offset="40%" stop-color="#FFA500"/>
      <stop offset="100%" stop-color="#FF7700"/>
    </linearGradient>
  </defs>

  <!-- Squircle background -->
  <rect x="32" y="32" width="448" height="448" rx="96" fill="url(#brandGrad)"/>

  <!-- Central hub ring -->
  <circle cx="256" cy="276" r="78" stroke="#000" stroke-width="28" fill="none" opacity="0.85"/>

  <!-- Hub core dot -->
  <circle cx="256" cy="276" r="22" fill="#000" opacity="0.85"/>

  <!-- Top spoke (connects to i-dot) -->
  <rect x="244" y="132" width="24" height="86" rx="12" fill="#000" opacity="0.85"/>
  <!-- Bottom spoke -->
  <rect x="244" y="338" width="24" height="64" rx="12" fill="#000" opacity="0.85"/>
  <!-- Left spoke -->
  <rect x="108" y="264" width="70" height="24" rx="12" fill="#000" opacity="0.85"/>
  <!-- Right spoke -->
  <rect x="334" y="264" width="70" height="24" rx="12" fill="#000" opacity="0.85"/>

  <!-- i-dot — the information node -->
  <circle cx="256" cy="116" r="28" fill="#000" opacity="0.85"/>

  <!-- Outer endpoint nodes -->
  <circle cx="104" cy="276" r="14" fill="#000" opacity="0.6"/>
  <circle cx="408" cy="276" r="14" fill="#000" opacity="0.6"/>
  <circle cx="256" cy="402" r="14" fill="#000" opacity="0.6"/>
</svg>
