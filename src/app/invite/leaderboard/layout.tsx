import { pageMetadata } from '@/lib/seo';
export const metadata = pageMetadata('/invite/leaderboard');
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
