import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/developers/webhooks');

export default function WebhooksGuideLayout({ children }: { children: React.ReactNode }) {
  return children;
}
