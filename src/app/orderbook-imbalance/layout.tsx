import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/orderbook-imbalance');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
