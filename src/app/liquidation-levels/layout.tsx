import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/liquidation-levels');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
