import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/liquidation-map');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
