import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/stablecoin-flows');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
