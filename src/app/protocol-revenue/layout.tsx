import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/protocol-revenue');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
