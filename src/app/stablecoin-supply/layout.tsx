import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/stablecoin-supply');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
