import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/earnings-calendar');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
