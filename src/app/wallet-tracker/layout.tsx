import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/wallet-tracker');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
