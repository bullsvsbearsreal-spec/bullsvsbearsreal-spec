import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/memecoin-radar');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
