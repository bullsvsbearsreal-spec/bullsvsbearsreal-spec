import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/perp-dex-volume');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
