import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/market-heatmap');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
