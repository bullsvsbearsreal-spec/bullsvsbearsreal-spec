import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/positions/simulate');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
