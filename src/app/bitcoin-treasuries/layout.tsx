import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/bitcoin-treasuries');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
