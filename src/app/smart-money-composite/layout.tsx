import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/smart-money-composite');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
