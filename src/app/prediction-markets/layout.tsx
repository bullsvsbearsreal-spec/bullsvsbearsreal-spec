import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/prediction-markets');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
