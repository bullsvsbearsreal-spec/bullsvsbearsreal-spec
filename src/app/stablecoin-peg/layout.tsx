import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/stablecoin-peg');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
