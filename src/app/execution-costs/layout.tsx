import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/execution-costs');

export default function ExecutionCostsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
