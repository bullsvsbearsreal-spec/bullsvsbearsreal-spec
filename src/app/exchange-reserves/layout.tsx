import { pageMetadata } from '@/lib/seo';

export const metadata = pageMetadata('/exchange-reserves');

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
