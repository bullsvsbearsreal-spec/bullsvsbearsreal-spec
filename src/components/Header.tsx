// Stubbed — chrome now comes from <ConditionalTerminalShell> in app/layout.tsx.
// Kept as a no-op export so existing import sites don't need to be touched.
// (Original implementation lived here prior to e80aec0b's terminal redesign;
// check git history if you ever need the old markup back.)
export default function Header() {
  return null;
}
