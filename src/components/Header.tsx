// Stubbed — chrome now comes from <ConditionalTerminalShell> in app/layout.tsx.
// Original implementation backed up at Header.full.tsx.bak in case we need to revert.
export default function Header() {
  return null;
}
