// Stubbed — chrome now comes from <ConditionalTerminalShell> + <StatusBar> in app/layout.tsx.
// Original implementation backed up at Footer.full.tsx.bak in case we need to revert.
export default function Footer() {
  return null;
}
